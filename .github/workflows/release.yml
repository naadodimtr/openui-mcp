name: Release
on:
  push:
    branches: [release]

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
      exists: ${{ steps.check.outputs.exists }}
    steps:
      - uses: actions/checkout@v6

      - name: Extract version
        id: version
        run: |
          VERSION=$(grep -oP 'const VERSION = "\K[^"]+' src/update.ts)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tag=v$VERSION" >> $GITHUB_OUTPUT

      - name: Check if tag exists
        id: check
        run: |
          if git ls-remote --tags origin "refs/tags/v${{ steps.version.outputs.version }}" | grep -q .; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Create tag
        if: steps.check.outputs.exists == 'false'
        run: |
          git tag "v${{ steps.version.outputs.version }}"
          git push origin "v${{ steps.version.outputs.version }}"

  build:
    needs: prepare
    if: needs.prepare.outputs.exists == 'false'
    strategy:
      matrix:
        include:
          - target: bun-linux-x64
            artifact: openui-mcp-linux-x64
            os: ubuntu-latest
          - target: bun-linux-arm64
            artifact: openui-mcp-linux-arm64
            os: ubuntu-latest
          - target: bun-darwin-arm64
            artifact: openui-mcp-darwin-arm64
            os: macos-latest
          - target: bun-darwin-x64
            artifact: openui-mcp-darwin-x64
            os: macos-latest
          - target: bun-windows-x64
            artifact: openui-mcp-win-x64
            os: windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Install previewer dependencies
        run: cd previewer && bun install

      - name: Build previewer
        run: cd previewer && bun run build

      - name: Embed assets
        run: bun scripts/embed-assets.ts

      - name: Compile binary
        run: bun build --compile src/server.ts --target=${{ matrix.target }} --outfile ${{ matrix.artifact }}

      - name: Build Kumo adapter
        run: bun adapters/kumo/build.ts

      - name: Rename binary (Unix)
        if: runner.os != 'Windows'
        run: mv ${{ matrix.artifact }} openui-mcp

      - name: Rename binary (Windows)
        if: runner.os == 'Windows'
        run: ren ${{ matrix.artifact }}.exe openui-mcp.exe

      - name: Stage and package (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p _stage/kumo
          cp openui-mcp _stage/
          cp -r adapters/kumo/dist/* _stage/kumo/
          tar -czf ${{ matrix.artifact }}.tar.gz -C _stage .

      - name: Stage and package (Windows)
        if: runner.os == 'Windows'
        run: |
          mkdir _stage\kumo
          copy openui-mcp.exe _stage\
          xcopy adapters\kumo\dist _stage\kumo\ /E /Y
          Compress-Archive -Path _stage\* -DestinationPath "${{ matrix.artifact }}.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: |
            ${{ matrix.artifact }}.tar.gz
            ${{ matrix.artifact }}.zip

  release:
    needs: [prepare, build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ needs.prepare.outputs.tag }}
          generate_release_notes: true
          files: |
            artifacts/**/*.tar.gz
            artifacts/**/*.zip
