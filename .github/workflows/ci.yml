name: CI
on:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2

      - name: Install root dependencies
        run: bun install

      - name: Install previewer dependencies
        run: cd previewer && bun install

      - name: Build previewer
        run: cd previewer && bun run build

      - name: Run unit tests
        run: bun test

      - name: Build Kumo adapter
        run: bun adapters/kumo/build.ts

      - name: Run Kumo renderer unit tests
        run: bun test ./adapters/kumo/tests/renderer.test.ts

      - name: Verify build compiles
        run: |
          bun scripts/embed-assets.ts
          bun build --compile src/server.ts --outfile dist/openui-mcp

  e2e:
    needs: test
    uses: ./.github/workflows/e2e.yml
